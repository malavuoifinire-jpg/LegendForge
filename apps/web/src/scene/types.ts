/** Pedina mostrata sul canvas. La posizione è il centro, in pixel immagine. */
export interface DemoToken {
  id: string;
  name: string;
  x: number;
  y: number;
  sizeInCells: number;
  color: string;
}
